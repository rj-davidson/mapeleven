// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"mapeleven/db/ent/coach"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CoachCreate is the builder for creating a Coach entity.
type CoachCreate struct {
	config
	mutation *CoachMutation
	hooks    []Hook
}

// SetFootballApiId sets the "footballApiId" field.
func (cc *CoachCreate) SetFootballApiId(i int) *CoachCreate {
	cc.mutation.SetFootballApiId(i)
	return cc
}

// SetName sets the "name" field.
func (cc *CoachCreate) SetName(s string) *CoachCreate {
	cc.mutation.SetName(s)
	return cc
}

// SetPhoto sets the "photo" field.
func (cc *CoachCreate) SetPhoto(s string) *CoachCreate {
	cc.mutation.SetPhoto(s)
	return cc
}

// SetLastUpdated sets the "lastUpdated" field.
func (cc *CoachCreate) SetLastUpdated(t time.Time) *CoachCreate {
	cc.mutation.SetLastUpdated(t)
	return cc
}

// SetNillableLastUpdated sets the "lastUpdated" field if the given value is not nil.
func (cc *CoachCreate) SetNillableLastUpdated(t *time.Time) *CoachCreate {
	if t != nil {
		cc.SetLastUpdated(*t)
	}
	return cc
}

// Mutation returns the CoachMutation object of the builder.
func (cc *CoachCreate) Mutation() *CoachMutation {
	return cc.mutation
}

// Save creates the Coach in the database.
func (cc *CoachCreate) Save(ctx context.Context) (*Coach, error) {
	cc.defaults()
	return withHooks[*Coach, CoachMutation](ctx, cc.sqlSave, cc.mutation, cc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (cc *CoachCreate) SaveX(ctx context.Context) *Coach {
	v, err := cc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cc *CoachCreate) Exec(ctx context.Context) error {
	_, err := cc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cc *CoachCreate) ExecX(ctx context.Context) {
	if err := cc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (cc *CoachCreate) defaults() {
	if _, ok := cc.mutation.LastUpdated(); !ok {
		v := coach.DefaultLastUpdated()
		cc.mutation.SetLastUpdated(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cc *CoachCreate) check() error {
	if _, ok := cc.mutation.FootballApiId(); !ok {
		return &ValidationError{Name: "footballApiId", err: errors.New(`ent: missing required field "Coach.footballApiId"`)}
	}
	if _, ok := cc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Coach.name"`)}
	}
	if _, ok := cc.mutation.Photo(); !ok {
		return &ValidationError{Name: "photo", err: errors.New(`ent: missing required field "Coach.photo"`)}
	}
	return nil
}

func (cc *CoachCreate) sqlSave(ctx context.Context) (*Coach, error) {
	if err := cc.check(); err != nil {
		return nil, err
	}
	_node, _spec := cc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	cc.mutation.id = &_node.ID
	cc.mutation.done = true
	return _node, nil
}

func (cc *CoachCreate) createSpec() (*Coach, *sqlgraph.CreateSpec) {
	var (
		_node = &Coach{config: cc.config}
		_spec = sqlgraph.NewCreateSpec(coach.Table, sqlgraph.NewFieldSpec(coach.FieldID, field.TypeInt))
	)
	if value, ok := cc.mutation.FootballApiId(); ok {
		_spec.SetField(coach.FieldFootballApiId, field.TypeInt, value)
		_node.FootballApiId = value
	}
	if value, ok := cc.mutation.Name(); ok {
		_spec.SetField(coach.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := cc.mutation.Photo(); ok {
		_spec.SetField(coach.FieldPhoto, field.TypeString, value)
		_node.Photo = value
	}
	if value, ok := cc.mutation.LastUpdated(); ok {
		_spec.SetField(coach.FieldLastUpdated, field.TypeTime, value)
		_node.LastUpdated = value
	}
	return _node, _spec
}

// CoachCreateBulk is the builder for creating many Coach entities in bulk.
type CoachCreateBulk struct {
	config
	builders []*CoachCreate
}

// Save creates the Coach entities in the database.
func (ccb *CoachCreateBulk) Save(ctx context.Context) ([]*Coach, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ccb.builders))
	nodes := make([]*Coach, len(ccb.builders))
	mutators := make([]Mutator, len(ccb.builders))
	for i := range ccb.builders {
		func(i int, root context.Context) {
			builder := ccb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CoachMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ccb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ccb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ccb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ccb *CoachCreateBulk) SaveX(ctx context.Context) []*Coach {
	v, err := ccb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ccb *CoachCreateBulk) Exec(ctx context.Context) error {
	_, err := ccb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ccb *CoachCreateBulk) ExecX(ctx context.Context) {
	if err := ccb.Exec(ctx); err != nil {
		panic(err)
	}
}
