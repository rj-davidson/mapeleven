// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"mapeleven/db/ent/club"
	"mapeleven/db/ent/playerseason"
	"mapeleven/db/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PlayerSeasonQuery is the builder for querying PlayerSeason entities.
type PlayerSeasonQuery struct {
	config
	ctx        *QueryContext
	order      []playerseason.Order
	inters     []Interceptor
	predicates []predicate.PlayerSeason
	withClub   *ClubQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlayerSeasonQuery builder.
func (psq *PlayerSeasonQuery) Where(ps ...predicate.PlayerSeason) *PlayerSeasonQuery {
	psq.predicates = append(psq.predicates, ps...)
	return psq
}

// Limit the number of records to be returned by this query.
func (psq *PlayerSeasonQuery) Limit(limit int) *PlayerSeasonQuery {
	psq.ctx.Limit = &limit
	return psq
}

// Offset to start from.
func (psq *PlayerSeasonQuery) Offset(offset int) *PlayerSeasonQuery {
	psq.ctx.Offset = &offset
	return psq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (psq *PlayerSeasonQuery) Unique(unique bool) *PlayerSeasonQuery {
	psq.ctx.Unique = &unique
	return psq
}

// Order specifies how the records should be ordered.
func (psq *PlayerSeasonQuery) Order(o ...playerseason.Order) *PlayerSeasonQuery {
	psq.order = append(psq.order, o...)
	return psq
}

// QueryClub chains the current query on the "club" edge.
func (psq *PlayerSeasonQuery) QueryClub() *ClubQuery {
	query := (&ClubClient{config: psq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := psq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := psq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playerseason.Table, playerseason.FieldID, selector),
			sqlgraph.To(club.Table, club.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, playerseason.ClubTable, playerseason.ClubPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(psq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PlayerSeason entity from the query.
// Returns a *NotFoundError when no PlayerSeason was found.
func (psq *PlayerSeasonQuery) First(ctx context.Context) (*PlayerSeason, error) {
	nodes, err := psq.Limit(1).All(setContextOp(ctx, psq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{playerseason.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (psq *PlayerSeasonQuery) FirstX(ctx context.Context) *PlayerSeason {
	node, err := psq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlayerSeason ID from the query.
// Returns a *NotFoundError when no PlayerSeason ID was found.
func (psq *PlayerSeasonQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = psq.Limit(1).IDs(setContextOp(ctx, psq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{playerseason.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (psq *PlayerSeasonQuery) FirstIDX(ctx context.Context) int {
	id, err := psq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlayerSeason entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlayerSeason entity is found.
// Returns a *NotFoundError when no PlayerSeason entities are found.
func (psq *PlayerSeasonQuery) Only(ctx context.Context) (*PlayerSeason, error) {
	nodes, err := psq.Limit(2).All(setContextOp(ctx, psq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{playerseason.Label}
	default:
		return nil, &NotSingularError{playerseason.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (psq *PlayerSeasonQuery) OnlyX(ctx context.Context) *PlayerSeason {
	node, err := psq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlayerSeason ID in the query.
// Returns a *NotSingularError when more than one PlayerSeason ID is found.
// Returns a *NotFoundError when no entities are found.
func (psq *PlayerSeasonQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = psq.Limit(2).IDs(setContextOp(ctx, psq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{playerseason.Label}
	default:
		err = &NotSingularError{playerseason.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (psq *PlayerSeasonQuery) OnlyIDX(ctx context.Context) int {
	id, err := psq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlayerSeasons.
func (psq *PlayerSeasonQuery) All(ctx context.Context) ([]*PlayerSeason, error) {
	ctx = setContextOp(ctx, psq.ctx, "All")
	if err := psq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PlayerSeason, *PlayerSeasonQuery]()
	return withInterceptors[[]*PlayerSeason](ctx, psq, qr, psq.inters)
}

// AllX is like All, but panics if an error occurs.
func (psq *PlayerSeasonQuery) AllX(ctx context.Context) []*PlayerSeason {
	nodes, err := psq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlayerSeason IDs.
func (psq *PlayerSeasonQuery) IDs(ctx context.Context) (ids []int, err error) {
	if psq.ctx.Unique == nil && psq.path != nil {
		psq.Unique(true)
	}
	ctx = setContextOp(ctx, psq.ctx, "IDs")
	if err = psq.Select(playerseason.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (psq *PlayerSeasonQuery) IDsX(ctx context.Context) []int {
	ids, err := psq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (psq *PlayerSeasonQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, psq.ctx, "Count")
	if err := psq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, psq, querierCount[*PlayerSeasonQuery](), psq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (psq *PlayerSeasonQuery) CountX(ctx context.Context) int {
	count, err := psq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (psq *PlayerSeasonQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, psq.ctx, "Exist")
	switch _, err := psq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (psq *PlayerSeasonQuery) ExistX(ctx context.Context) bool {
	exist, err := psq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlayerSeasonQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (psq *PlayerSeasonQuery) Clone() *PlayerSeasonQuery {
	if psq == nil {
		return nil
	}
	return &PlayerSeasonQuery{
		config:     psq.config,
		ctx:        psq.ctx.Clone(),
		order:      append([]playerseason.Order{}, psq.order...),
		inters:     append([]Interceptor{}, psq.inters...),
		predicates: append([]predicate.PlayerSeason{}, psq.predicates...),
		withClub:   psq.withClub.Clone(),
		// clone intermediate query.
		sql:  psq.sql.Clone(),
		path: psq.path,
	}
}

// WithClub tells the query-builder to eager-load the nodes that are connected to
// the "club" edge. The optional arguments are used to configure the query builder of the edge.
func (psq *PlayerSeasonQuery) WithClub(opts ...func(*ClubQuery)) *PlayerSeasonQuery {
	query := (&ClubClient{config: psq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	psq.withClub = query
	return psq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Season int `json:"season,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlayerSeason.Query().
//		GroupBy(playerseason.FieldSeason).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (psq *PlayerSeasonQuery) GroupBy(field string, fields ...string) *PlayerSeasonGroupBy {
	psq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PlayerSeasonGroupBy{build: psq}
	grbuild.flds = &psq.ctx.Fields
	grbuild.label = playerseason.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Season int `json:"season,omitempty"`
//	}
//
//	client.PlayerSeason.Query().
//		Select(playerseason.FieldSeason).
//		Scan(ctx, &v)
func (psq *PlayerSeasonQuery) Select(fields ...string) *PlayerSeasonSelect {
	psq.ctx.Fields = append(psq.ctx.Fields, fields...)
	sbuild := &PlayerSeasonSelect{PlayerSeasonQuery: psq}
	sbuild.label = playerseason.Label
	sbuild.flds, sbuild.scan = &psq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlayerSeasonSelect configured with the given aggregations.
func (psq *PlayerSeasonQuery) Aggregate(fns ...AggregateFunc) *PlayerSeasonSelect {
	return psq.Select().Aggregate(fns...)
}

func (psq *PlayerSeasonQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range psq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, psq); err != nil {
				return err
			}
		}
	}
	for _, f := range psq.ctx.Fields {
		if !playerseason.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if psq.path != nil {
		prev, err := psq.path(ctx)
		if err != nil {
			return err
		}
		psq.sql = prev
	}
	return nil
}

func (psq *PlayerSeasonQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlayerSeason, error) {
	var (
		nodes       = []*PlayerSeason{}
		withFKs     = psq.withFKs
		_spec       = psq.querySpec()
		loadedTypes = [1]bool{
			psq.withClub != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, playerseason.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlayerSeason).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlayerSeason{config: psq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, psq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := psq.withClub; query != nil {
		if err := psq.loadClub(ctx, query, nodes,
			func(n *PlayerSeason) { n.Edges.Club = []*Club{} },
			func(n *PlayerSeason, e *Club) { n.Edges.Club = append(n.Edges.Club, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (psq *PlayerSeasonQuery) loadClub(ctx context.Context, query *ClubQuery, nodes []*PlayerSeason, init func(*PlayerSeason), assign func(*PlayerSeason, *Club)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*PlayerSeason)
	nids := make(map[int]map[*PlayerSeason]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(playerseason.ClubTable)
		s.Join(joinT).On(s.C(club.FieldID), joinT.C(playerseason.ClubPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(playerseason.ClubPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(playerseason.ClubPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*PlayerSeason]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Club](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "club" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (psq *PlayerSeasonQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := psq.querySpec()
	_spec.Node.Columns = psq.ctx.Fields
	if len(psq.ctx.Fields) > 0 {
		_spec.Unique = psq.ctx.Unique != nil && *psq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, psq.driver, _spec)
}

func (psq *PlayerSeasonQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(playerseason.Table, playerseason.Columns, sqlgraph.NewFieldSpec(playerseason.FieldID, field.TypeInt))
	_spec.From = psq.sql
	if unique := psq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if psq.path != nil {
		_spec.Unique = true
	}
	if fields := psq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, playerseason.FieldID)
		for i := range fields {
			if fields[i] != playerseason.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := psq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := psq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := psq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := psq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (psq *PlayerSeasonQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(psq.driver.Dialect())
	t1 := builder.Table(playerseason.Table)
	columns := psq.ctx.Fields
	if len(columns) == 0 {
		columns = playerseason.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if psq.sql != nil {
		selector = psq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if psq.ctx.Unique != nil && *psq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range psq.predicates {
		p(selector)
	}
	for _, p := range psq.order {
		p(selector)
	}
	if offset := psq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := psq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PlayerSeasonGroupBy is the group-by builder for PlayerSeason entities.
type PlayerSeasonGroupBy struct {
	selector
	build *PlayerSeasonQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (psgb *PlayerSeasonGroupBy) Aggregate(fns ...AggregateFunc) *PlayerSeasonGroupBy {
	psgb.fns = append(psgb.fns, fns...)
	return psgb
}

// Scan applies the selector query and scans the result into the given value.
func (psgb *PlayerSeasonGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, psgb.build.ctx, "GroupBy")
	if err := psgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlayerSeasonQuery, *PlayerSeasonGroupBy](ctx, psgb.build, psgb, psgb.build.inters, v)
}

func (psgb *PlayerSeasonGroupBy) sqlScan(ctx context.Context, root *PlayerSeasonQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(psgb.fns))
	for _, fn := range psgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*psgb.flds)+len(psgb.fns))
		for _, f := range *psgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*psgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := psgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PlayerSeasonSelect is the builder for selecting fields of PlayerSeason entities.
type PlayerSeasonSelect struct {
	*PlayerSeasonQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pss *PlayerSeasonSelect) Aggregate(fns ...AggregateFunc) *PlayerSeasonSelect {
	pss.fns = append(pss.fns, fns...)
	return pss
}

// Scan applies the selector query and scans the result into the given value.
func (pss *PlayerSeasonSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pss.ctx, "Select")
	if err := pss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlayerSeasonQuery, *PlayerSeasonSelect](ctx, pss.PlayerSeasonQuery, pss, pss.inters, v)
}

func (pss *PlayerSeasonSelect) sqlScan(ctx context.Context, root *PlayerSeasonQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pss.fns))
	for _, fn := range pss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
