// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"mapeleven/db/ent/coach"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Coach is the model entity for the Coach schema.
type Coach struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// FootballApiId holds the value of the "footballApiId" field.
	FootballApiId int `json:"footballApiId,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Photo holds the value of the "photo" field.
	Photo string `json:"photo,omitempty"`
	// LastUpdated holds the value of the "lastUpdated" field.
	LastUpdated  time.Time `json:"lastUpdated,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Coach) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case coach.FieldID, coach.FieldFootballApiId:
			values[i] = new(sql.NullInt64)
		case coach.FieldName, coach.FieldPhoto:
			values[i] = new(sql.NullString)
		case coach.FieldLastUpdated:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Coach fields.
func (c *Coach) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case coach.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			c.ID = int(value.Int64)
		case coach.FieldFootballApiId:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field footballApiId", values[i])
			} else if value.Valid {
				c.FootballApiId = int(value.Int64)
			}
		case coach.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				c.Name = value.String
			}
		case coach.FieldPhoto:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field photo", values[i])
			} else if value.Valid {
				c.Photo = value.String
			}
		case coach.FieldLastUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field lastUpdated", values[i])
			} else if value.Valid {
				c.LastUpdated = value.Time
			}
		default:
			c.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Coach.
// This includes values selected through modifiers, order, etc.
func (c *Coach) Value(name string) (ent.Value, error) {
	return c.selectValues.Get(name)
}

// Update returns a builder for updating this Coach.
// Note that you need to call Coach.Unwrap() before calling this method if this Coach
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Coach) Update() *CoachUpdateOne {
	return NewCoachClient(c.config).UpdateOne(c)
}

// Unwrap unwraps the Coach entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Coach) Unwrap() *Coach {
	_tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Coach is not a transactional entity")
	}
	c.config.driver = _tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Coach) String() string {
	var builder strings.Builder
	builder.WriteString("Coach(")
	builder.WriteString(fmt.Sprintf("id=%v, ", c.ID))
	builder.WriteString("footballApiId=")
	builder.WriteString(fmt.Sprintf("%v", c.FootballApiId))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(c.Name)
	builder.WriteString(", ")
	builder.WriteString("photo=")
	builder.WriteString(c.Photo)
	builder.WriteString(", ")
	builder.WriteString("lastUpdated=")
	builder.WriteString(c.LastUpdated.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Coaches is a parsable slice of Coach.
type Coaches []*Coach
