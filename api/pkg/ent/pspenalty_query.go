// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/playerstats"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/predicate"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/pspenalty"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PSPenaltyQuery is the builder for querying PSPenalty entities.
type PSPenaltyQuery struct {
	config
	ctx             *QueryContext
	order           []pspenalty.OrderOption
	inters          []Interceptor
	predicates      []predicate.PSPenalty
	withPlayerStats *PlayerStatsQuery
	withFKs         bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PSPenaltyQuery builder.
func (ppq *PSPenaltyQuery) Where(ps ...predicate.PSPenalty) *PSPenaltyQuery {
	ppq.predicates = append(ppq.predicates, ps...)
	return ppq
}

// Limit the number of records to be returned by this query.
func (ppq *PSPenaltyQuery) Limit(limit int) *PSPenaltyQuery {
	ppq.ctx.Limit = &limit
	return ppq
}

// Offset to start from.
func (ppq *PSPenaltyQuery) Offset(offset int) *PSPenaltyQuery {
	ppq.ctx.Offset = &offset
	return ppq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ppq *PSPenaltyQuery) Unique(unique bool) *PSPenaltyQuery {
	ppq.ctx.Unique = &unique
	return ppq
}

// Order specifies how the records should be ordered.
func (ppq *PSPenaltyQuery) Order(o ...pspenalty.OrderOption) *PSPenaltyQuery {
	ppq.order = append(ppq.order, o...)
	return ppq
}

// QueryPlayerStats chains the current query on the "playerStats" edge.
func (ppq *PSPenaltyQuery) QueryPlayerStats() *PlayerStatsQuery {
	query := (&PlayerStatsClient{config: ppq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ppq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ppq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pspenalty.Table, pspenalty.FieldID, selector),
			sqlgraph.To(playerstats.Table, playerstats.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, pspenalty.PlayerStatsTable, pspenalty.PlayerStatsColumn),
		)
		fromU = sqlgraph.SetNeighbors(ppq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PSPenalty entity from the query.
// Returns a *NotFoundError when no PSPenalty was found.
func (ppq *PSPenaltyQuery) First(ctx context.Context) (*PSPenalty, error) {
	nodes, err := ppq.Limit(1).All(setContextOp(ctx, ppq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pspenalty.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ppq *PSPenaltyQuery) FirstX(ctx context.Context) *PSPenalty {
	node, err := ppq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PSPenalty ID from the query.
// Returns a *NotFoundError when no PSPenalty ID was found.
func (ppq *PSPenaltyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ppq.Limit(1).IDs(setContextOp(ctx, ppq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pspenalty.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ppq *PSPenaltyQuery) FirstIDX(ctx context.Context) int {
	id, err := ppq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PSPenalty entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PSPenalty entity is found.
// Returns a *NotFoundError when no PSPenalty entities are found.
func (ppq *PSPenaltyQuery) Only(ctx context.Context) (*PSPenalty, error) {
	nodes, err := ppq.Limit(2).All(setContextOp(ctx, ppq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pspenalty.Label}
	default:
		return nil, &NotSingularError{pspenalty.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ppq *PSPenaltyQuery) OnlyX(ctx context.Context) *PSPenalty {
	node, err := ppq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PSPenalty ID in the query.
// Returns a *NotSingularError when more than one PSPenalty ID is found.
// Returns a *NotFoundError when no entities are found.
func (ppq *PSPenaltyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ppq.Limit(2).IDs(setContextOp(ctx, ppq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pspenalty.Label}
	default:
		err = &NotSingularError{pspenalty.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ppq *PSPenaltyQuery) OnlyIDX(ctx context.Context) int {
	id, err := ppq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PSPenalties.
func (ppq *PSPenaltyQuery) All(ctx context.Context) ([]*PSPenalty, error) {
	ctx = setContextOp(ctx, ppq.ctx, "All")
	if err := ppq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PSPenalty, *PSPenaltyQuery]()
	return withInterceptors[[]*PSPenalty](ctx, ppq, qr, ppq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ppq *PSPenaltyQuery) AllX(ctx context.Context) []*PSPenalty {
	nodes, err := ppq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PSPenalty IDs.
func (ppq *PSPenaltyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ppq.ctx.Unique == nil && ppq.path != nil {
		ppq.Unique(true)
	}
	ctx = setContextOp(ctx, ppq.ctx, "IDs")
	if err = ppq.Select(pspenalty.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ppq *PSPenaltyQuery) IDsX(ctx context.Context) []int {
	ids, err := ppq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ppq *PSPenaltyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ppq.ctx, "Count")
	if err := ppq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ppq, querierCount[*PSPenaltyQuery](), ppq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ppq *PSPenaltyQuery) CountX(ctx context.Context) int {
	count, err := ppq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ppq *PSPenaltyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ppq.ctx, "Exist")
	switch _, err := ppq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ppq *PSPenaltyQuery) ExistX(ctx context.Context) bool {
	exist, err := ppq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PSPenaltyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ppq *PSPenaltyQuery) Clone() *PSPenaltyQuery {
	if ppq == nil {
		return nil
	}
	return &PSPenaltyQuery{
		config:          ppq.config,
		ctx:             ppq.ctx.Clone(),
		order:           append([]pspenalty.OrderOption{}, ppq.order...),
		inters:          append([]Interceptor{}, ppq.inters...),
		predicates:      append([]predicate.PSPenalty{}, ppq.predicates...),
		withPlayerStats: ppq.withPlayerStats.Clone(),
		// clone intermediate query.
		sql:  ppq.sql.Clone(),
		path: ppq.path,
	}
}

// WithPlayerStats tells the query-builder to eager-load the nodes that are connected to
// the "playerStats" edge. The optional arguments are used to configure the query builder of the edge.
func (ppq *PSPenaltyQuery) WithPlayerStats(opts ...func(*PlayerStatsQuery)) *PSPenaltyQuery {
	query := (&PlayerStatsClient{config: ppq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ppq.withPlayerStats = query
	return ppq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Won int `json:"Won,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PSPenalty.Query().
//		GroupBy(pspenalty.FieldWon).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ppq *PSPenaltyQuery) GroupBy(field string, fields ...string) *PSPenaltyGroupBy {
	ppq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PSPenaltyGroupBy{build: ppq}
	grbuild.flds = &ppq.ctx.Fields
	grbuild.label = pspenalty.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Won int `json:"Won,omitempty"`
//	}
//
//	client.PSPenalty.Query().
//		Select(pspenalty.FieldWon).
//		Scan(ctx, &v)
func (ppq *PSPenaltyQuery) Select(fields ...string) *PSPenaltySelect {
	ppq.ctx.Fields = append(ppq.ctx.Fields, fields...)
	sbuild := &PSPenaltySelect{PSPenaltyQuery: ppq}
	sbuild.label = pspenalty.Label
	sbuild.flds, sbuild.scan = &ppq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PSPenaltySelect configured with the given aggregations.
func (ppq *PSPenaltyQuery) Aggregate(fns ...AggregateFunc) *PSPenaltySelect {
	return ppq.Select().Aggregate(fns...)
}

func (ppq *PSPenaltyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ppq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ppq); err != nil {
				return err
			}
		}
	}
	for _, f := range ppq.ctx.Fields {
		if !pspenalty.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ppq.path != nil {
		prev, err := ppq.path(ctx)
		if err != nil {
			return err
		}
		ppq.sql = prev
	}
	return nil
}

func (ppq *PSPenaltyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PSPenalty, error) {
	var (
		nodes       = []*PSPenalty{}
		withFKs     = ppq.withFKs
		_spec       = ppq.querySpec()
		loadedTypes = [1]bool{
			ppq.withPlayerStats != nil,
		}
	)
	if ppq.withPlayerStats != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, pspenalty.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PSPenalty).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PSPenalty{config: ppq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ppq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ppq.withPlayerStats; query != nil {
		if err := ppq.loadPlayerStats(ctx, query, nodes, nil,
			func(n *PSPenalty, e *PlayerStats) { n.Edges.PlayerStats = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ppq *PSPenaltyQuery) loadPlayerStats(ctx context.Context, query *PlayerStatsQuery, nodes []*PSPenalty, init func(*PSPenalty), assign func(*PSPenalty, *PlayerStats)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PSPenalty)
	for i := range nodes {
		if nodes[i].player_stats_ps_penalty == nil {
			continue
		}
		fk := *nodes[i].player_stats_ps_penalty
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(playerstats.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "player_stats_ps_penalty" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ppq *PSPenaltyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ppq.querySpec()
	_spec.Node.Columns = ppq.ctx.Fields
	if len(ppq.ctx.Fields) > 0 {
		_spec.Unique = ppq.ctx.Unique != nil && *ppq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ppq.driver, _spec)
}

func (ppq *PSPenaltyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pspenalty.Table, pspenalty.Columns, sqlgraph.NewFieldSpec(pspenalty.FieldID, field.TypeInt))
	_spec.From = ppq.sql
	if unique := ppq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ppq.path != nil {
		_spec.Unique = true
	}
	if fields := ppq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pspenalty.FieldID)
		for i := range fields {
			if fields[i] != pspenalty.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ppq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ppq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ppq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ppq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ppq *PSPenaltyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ppq.driver.Dialect())
	t1 := builder.Table(pspenalty.Table)
	columns := ppq.ctx.Fields
	if len(columns) == 0 {
		columns = pspenalty.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ppq.sql != nil {
		selector = ppq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ppq.ctx.Unique != nil && *ppq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ppq.predicates {
		p(selector)
	}
	for _, p := range ppq.order {
		p(selector)
	}
	if offset := ppq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ppq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PSPenaltyGroupBy is the group-by builder for PSPenalty entities.
type PSPenaltyGroupBy struct {
	selector
	build *PSPenaltyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ppgb *PSPenaltyGroupBy) Aggregate(fns ...AggregateFunc) *PSPenaltyGroupBy {
	ppgb.fns = append(ppgb.fns, fns...)
	return ppgb
}

// Scan applies the selector query and scans the result into the given value.
func (ppgb *PSPenaltyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ppgb.build.ctx, "GroupBy")
	if err := ppgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PSPenaltyQuery, *PSPenaltyGroupBy](ctx, ppgb.build, ppgb, ppgb.build.inters, v)
}

func (ppgb *PSPenaltyGroupBy) sqlScan(ctx context.Context, root *PSPenaltyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ppgb.fns))
	for _, fn := range ppgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ppgb.flds)+len(ppgb.fns))
		for _, f := range *ppgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ppgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ppgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PSPenaltySelect is the builder for selecting fields of PSPenalty entities.
type PSPenaltySelect struct {
	*PSPenaltyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pps *PSPenaltySelect) Aggregate(fns ...AggregateFunc) *PSPenaltySelect {
	pps.fns = append(pps.fns, fns...)
	return pps
}

// Scan applies the selector query and scans the result into the given value.
func (pps *PSPenaltySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pps.ctx, "Select")
	if err := pps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PSPenaltyQuery, *PSPenaltySelect](ctx, pps.PSPenaltyQuery, pps, pps.inters, v)
}

func (pps *PSPenaltySelect) sqlScan(ctx context.Context, root *PSPenaltyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pps.fns))
	for _, fn := range pps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
