// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/player"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/predicate"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/psgoals"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PSGoalsQuery is the builder for querying PSGoals entities.
type PSGoalsQuery struct {
	config
	ctx        *QueryContext
	order      []psgoals.OrderOption
	inters     []Interceptor
	predicates []predicate.PSGoals
	withPlayer *PlayerQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PSGoalsQuery builder.
func (pgq *PSGoalsQuery) Where(ps ...predicate.PSGoals) *PSGoalsQuery {
	pgq.predicates = append(pgq.predicates, ps...)
	return pgq
}

// Limit the number of records to be returned by this query.
func (pgq *PSGoalsQuery) Limit(limit int) *PSGoalsQuery {
	pgq.ctx.Limit = &limit
	return pgq
}

// Offset to start from.
func (pgq *PSGoalsQuery) Offset(offset int) *PSGoalsQuery {
	pgq.ctx.Offset = &offset
	return pgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pgq *PSGoalsQuery) Unique(unique bool) *PSGoalsQuery {
	pgq.ctx.Unique = &unique
	return pgq
}

// Order specifies how the records should be ordered.
func (pgq *PSGoalsQuery) Order(o ...psgoals.OrderOption) *PSGoalsQuery {
	pgq.order = append(pgq.order, o...)
	return pgq
}

// QueryPlayer chains the current query on the "player" edge.
func (pgq *PSGoalsQuery) QueryPlayer() *PlayerQuery {
	query := (&PlayerClient{config: pgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(psgoals.Table, psgoals.FieldID, selector),
			sqlgraph.To(player.Table, player.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, psgoals.PlayerTable, psgoals.PlayerColumn),
		)
		fromU = sqlgraph.SetNeighbors(pgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PSGoals entity from the query.
// Returns a *NotFoundError when no PSGoals was found.
func (pgq *PSGoalsQuery) First(ctx context.Context) (*PSGoals, error) {
	nodes, err := pgq.Limit(1).All(setContextOp(ctx, pgq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{psgoals.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pgq *PSGoalsQuery) FirstX(ctx context.Context) *PSGoals {
	node, err := pgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PSGoals ID from the query.
// Returns a *NotFoundError when no PSGoals ID was found.
func (pgq *PSGoalsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pgq.Limit(1).IDs(setContextOp(ctx, pgq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{psgoals.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pgq *PSGoalsQuery) FirstIDX(ctx context.Context) int {
	id, err := pgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PSGoals entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PSGoals entity is found.
// Returns a *NotFoundError when no PSGoals entities are found.
func (pgq *PSGoalsQuery) Only(ctx context.Context) (*PSGoals, error) {
	nodes, err := pgq.Limit(2).All(setContextOp(ctx, pgq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{psgoals.Label}
	default:
		return nil, &NotSingularError{psgoals.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pgq *PSGoalsQuery) OnlyX(ctx context.Context) *PSGoals {
	node, err := pgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PSGoals ID in the query.
// Returns a *NotSingularError when more than one PSGoals ID is found.
// Returns a *NotFoundError when no entities are found.
func (pgq *PSGoalsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pgq.Limit(2).IDs(setContextOp(ctx, pgq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{psgoals.Label}
	default:
		err = &NotSingularError{psgoals.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pgq *PSGoalsQuery) OnlyIDX(ctx context.Context) int {
	id, err := pgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PSGoalsSlice.
func (pgq *PSGoalsQuery) All(ctx context.Context) ([]*PSGoals, error) {
	ctx = setContextOp(ctx, pgq.ctx, "All")
	if err := pgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PSGoals, *PSGoalsQuery]()
	return withInterceptors[[]*PSGoals](ctx, pgq, qr, pgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pgq *PSGoalsQuery) AllX(ctx context.Context) []*PSGoals {
	nodes, err := pgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PSGoals IDs.
func (pgq *PSGoalsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pgq.ctx.Unique == nil && pgq.path != nil {
		pgq.Unique(true)
	}
	ctx = setContextOp(ctx, pgq.ctx, "IDs")
	if err = pgq.Select(psgoals.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pgq *PSGoalsQuery) IDsX(ctx context.Context) []int {
	ids, err := pgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pgq *PSGoalsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pgq.ctx, "Count")
	if err := pgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pgq, querierCount[*PSGoalsQuery](), pgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pgq *PSGoalsQuery) CountX(ctx context.Context) int {
	count, err := pgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pgq *PSGoalsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pgq.ctx, "Exist")
	switch _, err := pgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pgq *PSGoalsQuery) ExistX(ctx context.Context) bool {
	exist, err := pgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PSGoalsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pgq *PSGoalsQuery) Clone() *PSGoalsQuery {
	if pgq == nil {
		return nil
	}
	return &PSGoalsQuery{
		config:     pgq.config,
		ctx:        pgq.ctx.Clone(),
		order:      append([]psgoals.OrderOption{}, pgq.order...),
		inters:     append([]Interceptor{}, pgq.inters...),
		predicates: append([]predicate.PSGoals{}, pgq.predicates...),
		withPlayer: pgq.withPlayer.Clone(),
		// clone intermediate query.
		sql:  pgq.sql.Clone(),
		path: pgq.path,
	}
}

// WithPlayer tells the query-builder to eager-load the nodes that are connected to
// the "player" edge. The optional arguments are used to configure the query builder of the edge.
func (pgq *PSGoalsQuery) WithPlayer(opts ...func(*PlayerQuery)) *PSGoalsQuery {
	query := (&PlayerClient{config: pgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pgq.withPlayer = query
	return pgq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TotalGoals int `json:"totalGoals,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PSGoals.Query().
//		GroupBy(psgoals.FieldTotalGoals).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pgq *PSGoalsQuery) GroupBy(field string, fields ...string) *PSGoalsGroupBy {
	pgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PSGoalsGroupBy{build: pgq}
	grbuild.flds = &pgq.ctx.Fields
	grbuild.label = psgoals.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TotalGoals int `json:"totalGoals,omitempty"`
//	}
//
//	client.PSGoals.Query().
//		Select(psgoals.FieldTotalGoals).
//		Scan(ctx, &v)
func (pgq *PSGoalsQuery) Select(fields ...string) *PSGoalsSelect {
	pgq.ctx.Fields = append(pgq.ctx.Fields, fields...)
	sbuild := &PSGoalsSelect{PSGoalsQuery: pgq}
	sbuild.label = psgoals.Label
	sbuild.flds, sbuild.scan = &pgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PSGoalsSelect configured with the given aggregations.
func (pgq *PSGoalsQuery) Aggregate(fns ...AggregateFunc) *PSGoalsSelect {
	return pgq.Select().Aggregate(fns...)
}

func (pgq *PSGoalsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pgq); err != nil {
				return err
			}
		}
	}
	for _, f := range pgq.ctx.Fields {
		if !psgoals.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pgq.path != nil {
		prev, err := pgq.path(ctx)
		if err != nil {
			return err
		}
		pgq.sql = prev
	}
	return nil
}

func (pgq *PSGoalsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PSGoals, error) {
	var (
		nodes       = []*PSGoals{}
		withFKs     = pgq.withFKs
		_spec       = pgq.querySpec()
		loadedTypes = [1]bool{
			pgq.withPlayer != nil,
		}
	)
	if pgq.withPlayer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, psgoals.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PSGoals).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PSGoals{config: pgq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pgq.withPlayer; query != nil {
		if err := pgq.loadPlayer(ctx, query, nodes, nil,
			func(n *PSGoals, e *Player) { n.Edges.Player = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pgq *PSGoalsQuery) loadPlayer(ctx context.Context, query *PlayerQuery, nodes []*PSGoals, init func(*PSGoals), assign func(*PSGoals, *Player)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PSGoals)
	for i := range nodes {
		if nodes[i].player_psgoals == nil {
			continue
		}
		fk := *nodes[i].player_psgoals
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(player.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "player_psgoals" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pgq *PSGoalsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pgq.querySpec()
	_spec.Node.Columns = pgq.ctx.Fields
	if len(pgq.ctx.Fields) > 0 {
		_spec.Unique = pgq.ctx.Unique != nil && *pgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pgq.driver, _spec)
}

func (pgq *PSGoalsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(psgoals.Table, psgoals.Columns, sqlgraph.NewFieldSpec(psgoals.FieldID, field.TypeInt))
	_spec.From = pgq.sql
	if unique := pgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pgq.path != nil {
		_spec.Unique = true
	}
	if fields := pgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, psgoals.FieldID)
		for i := range fields {
			if fields[i] != psgoals.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pgq *PSGoalsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pgq.driver.Dialect())
	t1 := builder.Table(psgoals.Table)
	columns := pgq.ctx.Fields
	if len(columns) == 0 {
		columns = psgoals.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pgq.sql != nil {
		selector = pgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pgq.ctx.Unique != nil && *pgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pgq.predicates {
		p(selector)
	}
	for _, p := range pgq.order {
		p(selector)
	}
	if offset := pgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PSGoalsGroupBy is the group-by builder for PSGoals entities.
type PSGoalsGroupBy struct {
	selector
	build *PSGoalsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pggb *PSGoalsGroupBy) Aggregate(fns ...AggregateFunc) *PSGoalsGroupBy {
	pggb.fns = append(pggb.fns, fns...)
	return pggb
}

// Scan applies the selector query and scans the result into the given value.
func (pggb *PSGoalsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pggb.build.ctx, "GroupBy")
	if err := pggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PSGoalsQuery, *PSGoalsGroupBy](ctx, pggb.build, pggb, pggb.build.inters, v)
}

func (pggb *PSGoalsGroupBy) sqlScan(ctx context.Context, root *PSGoalsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pggb.fns))
	for _, fn := range pggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pggb.flds)+len(pggb.fns))
		for _, f := range *pggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PSGoalsSelect is the builder for selecting fields of PSGoals entities.
type PSGoalsSelect struct {
	*PSGoalsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pgs *PSGoalsSelect) Aggregate(fns ...AggregateFunc) *PSGoalsSelect {
	pgs.fns = append(pgs.fns, fns...)
	return pgs
}

// Scan applies the selector query and scans the result into the given value.
func (pgs *PSGoalsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgs.ctx, "Select")
	if err := pgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PSGoalsQuery, *PSGoalsSelect](ctx, pgs.PSGoalsQuery, pgs, pgs.inters, v)
}

func (pgs *PSGoalsSelect) sqlScan(ctx context.Context, root *PSGoalsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pgs.fns))
	for _, fn := range pgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
