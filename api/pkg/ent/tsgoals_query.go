// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/predicate"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/team"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/tsgoals"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TSGoalsQuery is the builder for querying TSGoals entities.
type TSGoalsQuery struct {
	config
	ctx        *QueryContext
	order      []tsgoals.OrderOption
	inters     []Interceptor
	predicates []predicate.TSGoals
	withTeam   *TeamQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TSGoalsQuery builder.
func (tgq *TSGoalsQuery) Where(ps ...predicate.TSGoals) *TSGoalsQuery {
	tgq.predicates = append(tgq.predicates, ps...)
	return tgq
}

// Limit the number of records to be returned by this query.
func (tgq *TSGoalsQuery) Limit(limit int) *TSGoalsQuery {
	tgq.ctx.Limit = &limit
	return tgq
}

// Offset to start from.
func (tgq *TSGoalsQuery) Offset(offset int) *TSGoalsQuery {
	tgq.ctx.Offset = &offset
	return tgq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tgq *TSGoalsQuery) Unique(unique bool) *TSGoalsQuery {
	tgq.ctx.Unique = &unique
	return tgq
}

// Order specifies how the records should be ordered.
func (tgq *TSGoalsQuery) Order(o ...tsgoals.OrderOption) *TSGoalsQuery {
	tgq.order = append(tgq.order, o...)
	return tgq
}

// QueryTeam chains the current query on the "team" edge.
func (tgq *TSGoalsQuery) QueryTeam() *TeamQuery {
	query := (&TeamClient{config: tgq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tgq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tgq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tsgoals.Table, tsgoals.FieldID, selector),
			sqlgraph.To(team.Table, team.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, tsgoals.TeamTable, tsgoals.TeamColumn),
		)
		fromU = sqlgraph.SetNeighbors(tgq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TSGoals entity from the query.
// Returns a *NotFoundError when no TSGoals was found.
func (tgq *TSGoalsQuery) First(ctx context.Context) (*TSGoals, error) {
	nodes, err := tgq.Limit(1).All(setContextOp(ctx, tgq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tsgoals.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tgq *TSGoalsQuery) FirstX(ctx context.Context) *TSGoals {
	node, err := tgq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TSGoals ID from the query.
// Returns a *NotFoundError when no TSGoals ID was found.
func (tgq *TSGoalsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tgq.Limit(1).IDs(setContextOp(ctx, tgq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tsgoals.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tgq *TSGoalsQuery) FirstIDX(ctx context.Context) int {
	id, err := tgq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TSGoals entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TSGoals entity is found.
// Returns a *NotFoundError when no TSGoals entities are found.
func (tgq *TSGoalsQuery) Only(ctx context.Context) (*TSGoals, error) {
	nodes, err := tgq.Limit(2).All(setContextOp(ctx, tgq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tsgoals.Label}
	default:
		return nil, &NotSingularError{tsgoals.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tgq *TSGoalsQuery) OnlyX(ctx context.Context) *TSGoals {
	node, err := tgq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TSGoals ID in the query.
// Returns a *NotSingularError when more than one TSGoals ID is found.
// Returns a *NotFoundError when no entities are found.
func (tgq *TSGoalsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tgq.Limit(2).IDs(setContextOp(ctx, tgq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tsgoals.Label}
	default:
		err = &NotSingularError{tsgoals.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tgq *TSGoalsQuery) OnlyIDX(ctx context.Context) int {
	id, err := tgq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TSGoalsSlice.
func (tgq *TSGoalsQuery) All(ctx context.Context) ([]*TSGoals, error) {
	ctx = setContextOp(ctx, tgq.ctx, "All")
	if err := tgq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TSGoals, *TSGoalsQuery]()
	return withInterceptors[[]*TSGoals](ctx, tgq, qr, tgq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tgq *TSGoalsQuery) AllX(ctx context.Context) []*TSGoals {
	nodes, err := tgq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TSGoals IDs.
func (tgq *TSGoalsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tgq.ctx.Unique == nil && tgq.path != nil {
		tgq.Unique(true)
	}
	ctx = setContextOp(ctx, tgq.ctx, "IDs")
	if err = tgq.Select(tsgoals.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tgq *TSGoalsQuery) IDsX(ctx context.Context) []int {
	ids, err := tgq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tgq *TSGoalsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tgq.ctx, "Count")
	if err := tgq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tgq, querierCount[*TSGoalsQuery](), tgq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tgq *TSGoalsQuery) CountX(ctx context.Context) int {
	count, err := tgq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tgq *TSGoalsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tgq.ctx, "Exist")
	switch _, err := tgq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tgq *TSGoalsQuery) ExistX(ctx context.Context) bool {
	exist, err := tgq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TSGoalsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tgq *TSGoalsQuery) Clone() *TSGoalsQuery {
	if tgq == nil {
		return nil
	}
	return &TSGoalsQuery{
		config:     tgq.config,
		ctx:        tgq.ctx.Clone(),
		order:      append([]tsgoals.OrderOption{}, tgq.order...),
		inters:     append([]Interceptor{}, tgq.inters...),
		predicates: append([]predicate.TSGoals{}, tgq.predicates...),
		withTeam:   tgq.withTeam.Clone(),
		// clone intermediate query.
		sql:  tgq.sql.Clone(),
		path: tgq.path,
	}
}

// WithTeam tells the query-builder to eager-load the nodes that are connected to
// the "team" edge. The optional arguments are used to configure the query builder of the edge.
func (tgq *TSGoalsQuery) WithTeam(opts ...func(*TeamQuery)) *TSGoalsQuery {
	query := (&TeamClient{config: tgq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tgq.withTeam = query
	return tgq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		GoalsForTotalHome int `json:"goalsForTotalHome,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TSGoals.Query().
//		GroupBy(tsgoals.FieldGoalsForTotalHome).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tgq *TSGoalsQuery) GroupBy(field string, fields ...string) *TSGoalsGroupBy {
	tgq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TSGoalsGroupBy{build: tgq}
	grbuild.flds = &tgq.ctx.Fields
	grbuild.label = tsgoals.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		GoalsForTotalHome int `json:"goalsForTotalHome,omitempty"`
//	}
//
//	client.TSGoals.Query().
//		Select(tsgoals.FieldGoalsForTotalHome).
//		Scan(ctx, &v)
func (tgq *TSGoalsQuery) Select(fields ...string) *TSGoalsSelect {
	tgq.ctx.Fields = append(tgq.ctx.Fields, fields...)
	sbuild := &TSGoalsSelect{TSGoalsQuery: tgq}
	sbuild.label = tsgoals.Label
	sbuild.flds, sbuild.scan = &tgq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TSGoalsSelect configured with the given aggregations.
func (tgq *TSGoalsQuery) Aggregate(fns ...AggregateFunc) *TSGoalsSelect {
	return tgq.Select().Aggregate(fns...)
}

func (tgq *TSGoalsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tgq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tgq); err != nil {
				return err
			}
		}
	}
	for _, f := range tgq.ctx.Fields {
		if !tsgoals.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tgq.path != nil {
		prev, err := tgq.path(ctx)
		if err != nil {
			return err
		}
		tgq.sql = prev
	}
	return nil
}

func (tgq *TSGoalsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TSGoals, error) {
	var (
		nodes       = []*TSGoals{}
		withFKs     = tgq.withFKs
		_spec       = tgq.querySpec()
		loadedTypes = [1]bool{
			tgq.withTeam != nil,
		}
	)
	if tgq.withTeam != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, tsgoals.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TSGoals).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TSGoals{config: tgq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tgq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tgq.withTeam; query != nil {
		if err := tgq.loadTeam(ctx, query, nodes, nil,
			func(n *TSGoals, e *Team) { n.Edges.Team = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tgq *TSGoalsQuery) loadTeam(ctx context.Context, query *TeamQuery, nodes []*TSGoals, init func(*TSGoals), assign func(*TSGoals, *Team)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TSGoals)
	for i := range nodes {
		if nodes[i].team_goals_stats == nil {
			continue
		}
		fk := *nodes[i].team_goals_stats
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(team.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "team_goals_stats" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tgq *TSGoalsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tgq.querySpec()
	_spec.Node.Columns = tgq.ctx.Fields
	if len(tgq.ctx.Fields) > 0 {
		_spec.Unique = tgq.ctx.Unique != nil && *tgq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tgq.driver, _spec)
}

func (tgq *TSGoalsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tsgoals.Table, tsgoals.Columns, sqlgraph.NewFieldSpec(tsgoals.FieldID, field.TypeInt))
	_spec.From = tgq.sql
	if unique := tgq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tgq.path != nil {
		_spec.Unique = true
	}
	if fields := tgq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tsgoals.FieldID)
		for i := range fields {
			if fields[i] != tsgoals.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tgq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tgq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tgq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tgq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tgq *TSGoalsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tgq.driver.Dialect())
	t1 := builder.Table(tsgoals.Table)
	columns := tgq.ctx.Fields
	if len(columns) == 0 {
		columns = tsgoals.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tgq.sql != nil {
		selector = tgq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tgq.ctx.Unique != nil && *tgq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tgq.predicates {
		p(selector)
	}
	for _, p := range tgq.order {
		p(selector)
	}
	if offset := tgq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tgq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TSGoalsGroupBy is the group-by builder for TSGoals entities.
type TSGoalsGroupBy struct {
	selector
	build *TSGoalsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tggb *TSGoalsGroupBy) Aggregate(fns ...AggregateFunc) *TSGoalsGroupBy {
	tggb.fns = append(tggb.fns, fns...)
	return tggb
}

// Scan applies the selector query and scans the result into the given value.
func (tggb *TSGoalsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tggb.build.ctx, "GroupBy")
	if err := tggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TSGoalsQuery, *TSGoalsGroupBy](ctx, tggb.build, tggb, tggb.build.inters, v)
}

func (tggb *TSGoalsGroupBy) sqlScan(ctx context.Context, root *TSGoalsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tggb.fns))
	for _, fn := range tggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tggb.flds)+len(tggb.fns))
		for _, f := range *tggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TSGoalsSelect is the builder for selecting fields of TSGoals entities.
type TSGoalsSelect struct {
	*TSGoalsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tgs *TSGoalsSelect) Aggregate(fns ...AggregateFunc) *TSGoalsSelect {
	tgs.fns = append(tgs.fns, fns...)
	return tgs
}

// Scan applies the selector query and scans the result into the given value.
func (tgs *TSGoalsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tgs.ctx, "Select")
	if err := tgs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TSGoalsQuery, *TSGoalsSelect](ctx, tgs.TSGoalsQuery, tgs, tgs.inters, v)
}

func (tgs *TSGoalsSelect) sqlScan(ctx context.Context, root *TSGoalsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tgs.fns))
	for _, fn := range tgs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tgs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tgs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
