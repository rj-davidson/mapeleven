// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/predicate"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/team"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/tsfailedtoscore"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TSFailedToScoreQuery is the builder for querying TSFailedToScore entities.
type TSFailedToScoreQuery struct {
	config
	ctx        *QueryContext
	order      []tsfailedtoscore.OrderOption
	inters     []Interceptor
	predicates []predicate.TSFailedToScore
	withTeam   *TeamQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TSFailedToScoreQuery builder.
func (tftsq *TSFailedToScoreQuery) Where(ps ...predicate.TSFailedToScore) *TSFailedToScoreQuery {
	tftsq.predicates = append(tftsq.predicates, ps...)
	return tftsq
}

// Limit the number of records to be returned by this query.
func (tftsq *TSFailedToScoreQuery) Limit(limit int) *TSFailedToScoreQuery {
	tftsq.ctx.Limit = &limit
	return tftsq
}

// Offset to start from.
func (tftsq *TSFailedToScoreQuery) Offset(offset int) *TSFailedToScoreQuery {
	tftsq.ctx.Offset = &offset
	return tftsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tftsq *TSFailedToScoreQuery) Unique(unique bool) *TSFailedToScoreQuery {
	tftsq.ctx.Unique = &unique
	return tftsq
}

// Order specifies how the records should be ordered.
func (tftsq *TSFailedToScoreQuery) Order(o ...tsfailedtoscore.OrderOption) *TSFailedToScoreQuery {
	tftsq.order = append(tftsq.order, o...)
	return tftsq
}

// QueryTeam chains the current query on the "team" edge.
func (tftsq *TSFailedToScoreQuery) QueryTeam() *TeamQuery {
	query := (&TeamClient{config: tftsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tftsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tftsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tsfailedtoscore.Table, tsfailedtoscore.FieldID, selector),
			sqlgraph.To(team.Table, team.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, tsfailedtoscore.TeamTable, tsfailedtoscore.TeamColumn),
		)
		fromU = sqlgraph.SetNeighbors(tftsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TSFailedToScore entity from the query.
// Returns a *NotFoundError when no TSFailedToScore was found.
func (tftsq *TSFailedToScoreQuery) First(ctx context.Context) (*TSFailedToScore, error) {
	nodes, err := tftsq.Limit(1).All(setContextOp(ctx, tftsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tsfailedtoscore.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) FirstX(ctx context.Context) *TSFailedToScore {
	node, err := tftsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TSFailedToScore ID from the query.
// Returns a *NotFoundError when no TSFailedToScore ID was found.
func (tftsq *TSFailedToScoreQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tftsq.Limit(1).IDs(setContextOp(ctx, tftsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tsfailedtoscore.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) FirstIDX(ctx context.Context) int {
	id, err := tftsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TSFailedToScore entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TSFailedToScore entity is found.
// Returns a *NotFoundError when no TSFailedToScore entities are found.
func (tftsq *TSFailedToScoreQuery) Only(ctx context.Context) (*TSFailedToScore, error) {
	nodes, err := tftsq.Limit(2).All(setContextOp(ctx, tftsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tsfailedtoscore.Label}
	default:
		return nil, &NotSingularError{tsfailedtoscore.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) OnlyX(ctx context.Context) *TSFailedToScore {
	node, err := tftsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TSFailedToScore ID in the query.
// Returns a *NotSingularError when more than one TSFailedToScore ID is found.
// Returns a *NotFoundError when no entities are found.
func (tftsq *TSFailedToScoreQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tftsq.Limit(2).IDs(setContextOp(ctx, tftsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tsfailedtoscore.Label}
	default:
		err = &NotSingularError{tsfailedtoscore.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) OnlyIDX(ctx context.Context) int {
	id, err := tftsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TSFailedToScores.
func (tftsq *TSFailedToScoreQuery) All(ctx context.Context) ([]*TSFailedToScore, error) {
	ctx = setContextOp(ctx, tftsq.ctx, "All")
	if err := tftsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TSFailedToScore, *TSFailedToScoreQuery]()
	return withInterceptors[[]*TSFailedToScore](ctx, tftsq, qr, tftsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) AllX(ctx context.Context) []*TSFailedToScore {
	nodes, err := tftsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TSFailedToScore IDs.
func (tftsq *TSFailedToScoreQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tftsq.ctx.Unique == nil && tftsq.path != nil {
		tftsq.Unique(true)
	}
	ctx = setContextOp(ctx, tftsq.ctx, "IDs")
	if err = tftsq.Select(tsfailedtoscore.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) IDsX(ctx context.Context) []int {
	ids, err := tftsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tftsq *TSFailedToScoreQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tftsq.ctx, "Count")
	if err := tftsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tftsq, querierCount[*TSFailedToScoreQuery](), tftsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) CountX(ctx context.Context) int {
	count, err := tftsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tftsq *TSFailedToScoreQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tftsq.ctx, "Exist")
	switch _, err := tftsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tftsq *TSFailedToScoreQuery) ExistX(ctx context.Context) bool {
	exist, err := tftsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TSFailedToScoreQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tftsq *TSFailedToScoreQuery) Clone() *TSFailedToScoreQuery {
	if tftsq == nil {
		return nil
	}
	return &TSFailedToScoreQuery{
		config:     tftsq.config,
		ctx:        tftsq.ctx.Clone(),
		order:      append([]tsfailedtoscore.OrderOption{}, tftsq.order...),
		inters:     append([]Interceptor{}, tftsq.inters...),
		predicates: append([]predicate.TSFailedToScore{}, tftsq.predicates...),
		withTeam:   tftsq.withTeam.Clone(),
		// clone intermediate query.
		sql:  tftsq.sql.Clone(),
		path: tftsq.path,
	}
}

// WithTeam tells the query-builder to eager-load the nodes that are connected to
// the "team" edge. The optional arguments are used to configure the query builder of the edge.
func (tftsq *TSFailedToScoreQuery) WithTeam(opts ...func(*TeamQuery)) *TSFailedToScoreQuery {
	query := (&TeamClient{config: tftsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tftsq.withTeam = query
	return tftsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Home int `json:"home,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TSFailedToScore.Query().
//		GroupBy(tsfailedtoscore.FieldHome).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tftsq *TSFailedToScoreQuery) GroupBy(field string, fields ...string) *TSFailedToScoreGroupBy {
	tftsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TSFailedToScoreGroupBy{build: tftsq}
	grbuild.flds = &tftsq.ctx.Fields
	grbuild.label = tsfailedtoscore.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Home int `json:"home,omitempty"`
//	}
//
//	client.TSFailedToScore.Query().
//		Select(tsfailedtoscore.FieldHome).
//		Scan(ctx, &v)
func (tftsq *TSFailedToScoreQuery) Select(fields ...string) *TSFailedToScoreSelect {
	tftsq.ctx.Fields = append(tftsq.ctx.Fields, fields...)
	sbuild := &TSFailedToScoreSelect{TSFailedToScoreQuery: tftsq}
	sbuild.label = tsfailedtoscore.Label
	sbuild.flds, sbuild.scan = &tftsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TSFailedToScoreSelect configured with the given aggregations.
func (tftsq *TSFailedToScoreQuery) Aggregate(fns ...AggregateFunc) *TSFailedToScoreSelect {
	return tftsq.Select().Aggregate(fns...)
}

func (tftsq *TSFailedToScoreQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tftsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tftsq); err != nil {
				return err
			}
		}
	}
	for _, f := range tftsq.ctx.Fields {
		if !tsfailedtoscore.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tftsq.path != nil {
		prev, err := tftsq.path(ctx)
		if err != nil {
			return err
		}
		tftsq.sql = prev
	}
	return nil
}

func (tftsq *TSFailedToScoreQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TSFailedToScore, error) {
	var (
		nodes       = []*TSFailedToScore{}
		withFKs     = tftsq.withFKs
		_spec       = tftsq.querySpec()
		loadedTypes = [1]bool{
			tftsq.withTeam != nil,
		}
	)
	if tftsq.withTeam != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, tsfailedtoscore.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TSFailedToScore).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TSFailedToScore{config: tftsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tftsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tftsq.withTeam; query != nil {
		if err := tftsq.loadTeam(ctx, query, nodes, nil,
			func(n *TSFailedToScore, e *Team) { n.Edges.Team = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tftsq *TSFailedToScoreQuery) loadTeam(ctx context.Context, query *TeamQuery, nodes []*TSFailedToScore, init func(*TSFailedToScore), assign func(*TSFailedToScore, *Team)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TSFailedToScore)
	for i := range nodes {
		if nodes[i].team_failed_to_score_stats == nil {
			continue
		}
		fk := *nodes[i].team_failed_to_score_stats
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(team.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "team_failed_to_score_stats" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tftsq *TSFailedToScoreQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tftsq.querySpec()
	_spec.Node.Columns = tftsq.ctx.Fields
	if len(tftsq.ctx.Fields) > 0 {
		_spec.Unique = tftsq.ctx.Unique != nil && *tftsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tftsq.driver, _spec)
}

func (tftsq *TSFailedToScoreQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tsfailedtoscore.Table, tsfailedtoscore.Columns, sqlgraph.NewFieldSpec(tsfailedtoscore.FieldID, field.TypeInt))
	_spec.From = tftsq.sql
	if unique := tftsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tftsq.path != nil {
		_spec.Unique = true
	}
	if fields := tftsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tsfailedtoscore.FieldID)
		for i := range fields {
			if fields[i] != tsfailedtoscore.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tftsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tftsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tftsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tftsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tftsq *TSFailedToScoreQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tftsq.driver.Dialect())
	t1 := builder.Table(tsfailedtoscore.Table)
	columns := tftsq.ctx.Fields
	if len(columns) == 0 {
		columns = tsfailedtoscore.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tftsq.sql != nil {
		selector = tftsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tftsq.ctx.Unique != nil && *tftsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tftsq.predicates {
		p(selector)
	}
	for _, p := range tftsq.order {
		p(selector)
	}
	if offset := tftsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tftsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TSFailedToScoreGroupBy is the group-by builder for TSFailedToScore entities.
type TSFailedToScoreGroupBy struct {
	selector
	build *TSFailedToScoreQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tftsgb *TSFailedToScoreGroupBy) Aggregate(fns ...AggregateFunc) *TSFailedToScoreGroupBy {
	tftsgb.fns = append(tftsgb.fns, fns...)
	return tftsgb
}

// Scan applies the selector query and scans the result into the given value.
func (tftsgb *TSFailedToScoreGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tftsgb.build.ctx, "GroupBy")
	if err := tftsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TSFailedToScoreQuery, *TSFailedToScoreGroupBy](ctx, tftsgb.build, tftsgb, tftsgb.build.inters, v)
}

func (tftsgb *TSFailedToScoreGroupBy) sqlScan(ctx context.Context, root *TSFailedToScoreQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tftsgb.fns))
	for _, fn := range tftsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tftsgb.flds)+len(tftsgb.fns))
		for _, f := range *tftsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tftsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tftsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TSFailedToScoreSelect is the builder for selecting fields of TSFailedToScore entities.
type TSFailedToScoreSelect struct {
	*TSFailedToScoreQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tftss *TSFailedToScoreSelect) Aggregate(fns ...AggregateFunc) *TSFailedToScoreSelect {
	tftss.fns = append(tftss.fns, fns...)
	return tftss
}

// Scan applies the selector query and scans the result into the given value.
func (tftss *TSFailedToScoreSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tftss.ctx, "Select")
	if err := tftss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TSFailedToScoreQuery, *TSFailedToScoreSelect](ctx, tftss.TSFailedToScoreQuery, tftss, tftss.inters, v)
}

func (tftss *TSFailedToScoreSelect) sqlScan(ctx context.Context, root *TSFailedToScoreQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tftss.fns))
	for _, fn := range tftss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tftss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tftss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
