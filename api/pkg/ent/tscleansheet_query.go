// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/predicate"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/team"
	"capstone-cs.eng.utah.edu/mapeleven/mapeleven/pkg/ent/tscleansheet"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// TSCleanSheetQuery is the builder for querying TSCleanSheet entities.
type TSCleanSheetQuery struct {
	config
	ctx        *QueryContext
	order      []tscleansheet.OrderOption
	inters     []Interceptor
	predicates []predicate.TSCleanSheet
	withTeam   *TeamQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TSCleanSheetQuery builder.
func (tcsq *TSCleanSheetQuery) Where(ps ...predicate.TSCleanSheet) *TSCleanSheetQuery {
	tcsq.predicates = append(tcsq.predicates, ps...)
	return tcsq
}

// Limit the number of records to be returned by this query.
func (tcsq *TSCleanSheetQuery) Limit(limit int) *TSCleanSheetQuery {
	tcsq.ctx.Limit = &limit
	return tcsq
}

// Offset to start from.
func (tcsq *TSCleanSheetQuery) Offset(offset int) *TSCleanSheetQuery {
	tcsq.ctx.Offset = &offset
	return tcsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tcsq *TSCleanSheetQuery) Unique(unique bool) *TSCleanSheetQuery {
	tcsq.ctx.Unique = &unique
	return tcsq
}

// Order specifies how the records should be ordered.
func (tcsq *TSCleanSheetQuery) Order(o ...tscleansheet.OrderOption) *TSCleanSheetQuery {
	tcsq.order = append(tcsq.order, o...)
	return tcsq
}

// QueryTeam chains the current query on the "team" edge.
func (tcsq *TSCleanSheetQuery) QueryTeam() *TeamQuery {
	query := (&TeamClient{config: tcsq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tcsq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tcsq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(tscleansheet.Table, tscleansheet.FieldID, selector),
			sqlgraph.To(team.Table, team.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, tscleansheet.TeamTable, tscleansheet.TeamColumn),
		)
		fromU = sqlgraph.SetNeighbors(tcsq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TSCleanSheet entity from the query.
// Returns a *NotFoundError when no TSCleanSheet was found.
func (tcsq *TSCleanSheetQuery) First(ctx context.Context) (*TSCleanSheet, error) {
	nodes, err := tcsq.Limit(1).All(setContextOp(ctx, tcsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{tscleansheet.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) FirstX(ctx context.Context) *TSCleanSheet {
	node, err := tcsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TSCleanSheet ID from the query.
// Returns a *NotFoundError when no TSCleanSheet ID was found.
func (tcsq *TSCleanSheetQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tcsq.Limit(1).IDs(setContextOp(ctx, tcsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{tscleansheet.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) FirstIDX(ctx context.Context) int {
	id, err := tcsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TSCleanSheet entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TSCleanSheet entity is found.
// Returns a *NotFoundError when no TSCleanSheet entities are found.
func (tcsq *TSCleanSheetQuery) Only(ctx context.Context) (*TSCleanSheet, error) {
	nodes, err := tcsq.Limit(2).All(setContextOp(ctx, tcsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{tscleansheet.Label}
	default:
		return nil, &NotSingularError{tscleansheet.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) OnlyX(ctx context.Context) *TSCleanSheet {
	node, err := tcsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TSCleanSheet ID in the query.
// Returns a *NotSingularError when more than one TSCleanSheet ID is found.
// Returns a *NotFoundError when no entities are found.
func (tcsq *TSCleanSheetQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tcsq.Limit(2).IDs(setContextOp(ctx, tcsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{tscleansheet.Label}
	default:
		err = &NotSingularError{tscleansheet.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) OnlyIDX(ctx context.Context) int {
	id, err := tcsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TSCleanSheets.
func (tcsq *TSCleanSheetQuery) All(ctx context.Context) ([]*TSCleanSheet, error) {
	ctx = setContextOp(ctx, tcsq.ctx, "All")
	if err := tcsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TSCleanSheet, *TSCleanSheetQuery]()
	return withInterceptors[[]*TSCleanSheet](ctx, tcsq, qr, tcsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) AllX(ctx context.Context) []*TSCleanSheet {
	nodes, err := tcsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TSCleanSheet IDs.
func (tcsq *TSCleanSheetQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tcsq.ctx.Unique == nil && tcsq.path != nil {
		tcsq.Unique(true)
	}
	ctx = setContextOp(ctx, tcsq.ctx, "IDs")
	if err = tcsq.Select(tscleansheet.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) IDsX(ctx context.Context) []int {
	ids, err := tcsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tcsq *TSCleanSheetQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tcsq.ctx, "Count")
	if err := tcsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tcsq, querierCount[*TSCleanSheetQuery](), tcsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) CountX(ctx context.Context) int {
	count, err := tcsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tcsq *TSCleanSheetQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tcsq.ctx, "Exist")
	switch _, err := tcsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tcsq *TSCleanSheetQuery) ExistX(ctx context.Context) bool {
	exist, err := tcsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TSCleanSheetQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tcsq *TSCleanSheetQuery) Clone() *TSCleanSheetQuery {
	if tcsq == nil {
		return nil
	}
	return &TSCleanSheetQuery{
		config:     tcsq.config,
		ctx:        tcsq.ctx.Clone(),
		order:      append([]tscleansheet.OrderOption{}, tcsq.order...),
		inters:     append([]Interceptor{}, tcsq.inters...),
		predicates: append([]predicate.TSCleanSheet{}, tcsq.predicates...),
		withTeam:   tcsq.withTeam.Clone(),
		// clone intermediate query.
		sql:  tcsq.sql.Clone(),
		path: tcsq.path,
	}
}

// WithTeam tells the query-builder to eager-load the nodes that are connected to
// the "team" edge. The optional arguments are used to configure the query builder of the edge.
func (tcsq *TSCleanSheetQuery) WithTeam(opts ...func(*TeamQuery)) *TSCleanSheetQuery {
	query := (&TeamClient{config: tcsq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tcsq.withTeam = query
	return tcsq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Home int `json:"home,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TSCleanSheet.Query().
//		GroupBy(tscleansheet.FieldHome).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tcsq *TSCleanSheetQuery) GroupBy(field string, fields ...string) *TSCleanSheetGroupBy {
	tcsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TSCleanSheetGroupBy{build: tcsq}
	grbuild.flds = &tcsq.ctx.Fields
	grbuild.label = tscleansheet.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Home int `json:"home,omitempty"`
//	}
//
//	client.TSCleanSheet.Query().
//		Select(tscleansheet.FieldHome).
//		Scan(ctx, &v)
func (tcsq *TSCleanSheetQuery) Select(fields ...string) *TSCleanSheetSelect {
	tcsq.ctx.Fields = append(tcsq.ctx.Fields, fields...)
	sbuild := &TSCleanSheetSelect{TSCleanSheetQuery: tcsq}
	sbuild.label = tscleansheet.Label
	sbuild.flds, sbuild.scan = &tcsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TSCleanSheetSelect configured with the given aggregations.
func (tcsq *TSCleanSheetQuery) Aggregate(fns ...AggregateFunc) *TSCleanSheetSelect {
	return tcsq.Select().Aggregate(fns...)
}

func (tcsq *TSCleanSheetQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tcsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tcsq); err != nil {
				return err
			}
		}
	}
	for _, f := range tcsq.ctx.Fields {
		if !tscleansheet.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tcsq.path != nil {
		prev, err := tcsq.path(ctx)
		if err != nil {
			return err
		}
		tcsq.sql = prev
	}
	return nil
}

func (tcsq *TSCleanSheetQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TSCleanSheet, error) {
	var (
		nodes       = []*TSCleanSheet{}
		withFKs     = tcsq.withFKs
		_spec       = tcsq.querySpec()
		loadedTypes = [1]bool{
			tcsq.withTeam != nil,
		}
	)
	if tcsq.withTeam != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, tscleansheet.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TSCleanSheet).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TSCleanSheet{config: tcsq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tcsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tcsq.withTeam; query != nil {
		if err := tcsq.loadTeam(ctx, query, nodes, nil,
			func(n *TSCleanSheet, e *Team) { n.Edges.Team = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tcsq *TSCleanSheetQuery) loadTeam(ctx context.Context, query *TeamQuery, nodes []*TSCleanSheet, init func(*TSCleanSheet), assign func(*TSCleanSheet, *Team)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TSCleanSheet)
	for i := range nodes {
		if nodes[i].team_clean_sheet_stats == nil {
			continue
		}
		fk := *nodes[i].team_clean_sheet_stats
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(team.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "team_clean_sheet_stats" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tcsq *TSCleanSheetQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tcsq.querySpec()
	_spec.Node.Columns = tcsq.ctx.Fields
	if len(tcsq.ctx.Fields) > 0 {
		_spec.Unique = tcsq.ctx.Unique != nil && *tcsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tcsq.driver, _spec)
}

func (tcsq *TSCleanSheetQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(tscleansheet.Table, tscleansheet.Columns, sqlgraph.NewFieldSpec(tscleansheet.FieldID, field.TypeInt))
	_spec.From = tcsq.sql
	if unique := tcsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tcsq.path != nil {
		_spec.Unique = true
	}
	if fields := tcsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, tscleansheet.FieldID)
		for i := range fields {
			if fields[i] != tscleansheet.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tcsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tcsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tcsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tcsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tcsq *TSCleanSheetQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tcsq.driver.Dialect())
	t1 := builder.Table(tscleansheet.Table)
	columns := tcsq.ctx.Fields
	if len(columns) == 0 {
		columns = tscleansheet.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tcsq.sql != nil {
		selector = tcsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tcsq.ctx.Unique != nil && *tcsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tcsq.predicates {
		p(selector)
	}
	for _, p := range tcsq.order {
		p(selector)
	}
	if offset := tcsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tcsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TSCleanSheetGroupBy is the group-by builder for TSCleanSheet entities.
type TSCleanSheetGroupBy struct {
	selector
	build *TSCleanSheetQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tcsgb *TSCleanSheetGroupBy) Aggregate(fns ...AggregateFunc) *TSCleanSheetGroupBy {
	tcsgb.fns = append(tcsgb.fns, fns...)
	return tcsgb
}

// Scan applies the selector query and scans the result into the given value.
func (tcsgb *TSCleanSheetGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tcsgb.build.ctx, "GroupBy")
	if err := tcsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TSCleanSheetQuery, *TSCleanSheetGroupBy](ctx, tcsgb.build, tcsgb, tcsgb.build.inters, v)
}

func (tcsgb *TSCleanSheetGroupBy) sqlScan(ctx context.Context, root *TSCleanSheetQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tcsgb.fns))
	for _, fn := range tcsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tcsgb.flds)+len(tcsgb.fns))
		for _, f := range *tcsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tcsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tcsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TSCleanSheetSelect is the builder for selecting fields of TSCleanSheet entities.
type TSCleanSheetSelect struct {
	*TSCleanSheetQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tcss *TSCleanSheetSelect) Aggregate(fns ...AggregateFunc) *TSCleanSheetSelect {
	tcss.fns = append(tcss.fns, fns...)
	return tcss
}

// Scan applies the selector query and scans the result into the given value.
func (tcss *TSCleanSheetSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tcss.ctx, "Select")
	if err := tcss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TSCleanSheetQuery, *TSCleanSheetSelect](ctx, tcss.TSCleanSheetQuery, tcss, tcss.inters, v)
}

func (tcss *TSCleanSheetSelect) sqlScan(ctx context.Context, root *TSCleanSheetQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tcss.fns))
	for _, fn := range tcss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tcss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tcss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
